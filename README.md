# AI Browser Agent

Double-click **`Start AI Browser Agent.cmd`**.

Lần đầu app sẽ hỏi **Gemini API key một lần** (lấy miễn phí tại https://aistudio.google.com/apikey) và lưu vào `local/gemini.key` — các lần sau không cần nhập lại.

Hoặc chạy thủ công: `Setup Gemini Key.cmd`
